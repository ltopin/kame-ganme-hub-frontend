export function SiteFooter() {
  return (
    <footer className="border-t border-border bg-hero px-4 py-8 text-hero-foreground/70">
      <div className="mx-auto max-w-5xl text-center font-mono text-xs tracking-wide">
        <p>Kame Game · Peça pelo WhatsApp</p>
        <p className="mt-1">Sem estoque fixo — cada peça é buscada sob encomenda.</p>
      </div>
    </footer>
  );
}
